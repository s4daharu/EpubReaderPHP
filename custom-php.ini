; custom-php.ini
upload_max_filesize = 50M  ; Adjust as needed
post_max_size = 55M        ; Should be slightly larger than upload_max_filesize
memory_limit = 256M        ; May need to increase if EPUB parsing is memory intensive